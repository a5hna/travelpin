
<div class="container">
  <h1><%= @board.title %></h1>
  <div class="user-avatars d-flex">
    <%= link_to board_board_users_path(@board) do  %>
    <% @board_users.each do |user| %>
    <img class="avatar" alt="avatar" src=<%= user.avatar %> />
    <% end %>
    <% end %>
    <%= link_to new_board_board_user_path(@board) do %>
    <i class="fas fa-plus-circle avatar"></i>
    <% end %>
  </div>
  <div class="map-placeholder card">
    <%= image_tag "https://images.unsplash.com/photo-1524661135-423995f22d0b?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=1653&q=80", alt: "alttext" %>
  </div>
  <div class="row justify-content-center">
    <% @experiences.each do |experience| %>
    <div class="col-xs-6 m-0">
      <div class="experience-card">
        <%= image_tag experience.photo, alt: "alttext" %>
        <p><%= experience.title %></p>
      </div>
    </div>
    <% end %>
  </div>
</div>
